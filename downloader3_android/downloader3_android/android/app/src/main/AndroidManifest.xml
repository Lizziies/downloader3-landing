<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet: Login/Premium/Downloads -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Für den späteren Downloader-Teil (Videos/Audio auf das Gerät
         speichern) -- ab Android 13 (API 33) reicht der scoped-storage-
         Zugriff über den System-Dateiwähler, ältere Versionen brauchen
         die klassische Berechtigung. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <!-- Für die Selbst-Update-Installation (APK von GitHub laden und
         direkt installieren, analog zum robocopy-Updater am Desktop). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:label="Downloader&lt;3"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false"
        android:requestLegacyExternalStorage="true"
        android:extractNativeLibs="true">
        <!-- 🎬 requestLegacyExternalStorage + extractNativeLibs: von
             youtubedl-android (Download-Motor, siehe DownloaderPlugin.kt)
             ausdrücklich so empfohlen -- ohne extractNativeLibs="true"
             findet Android die mitgebrachten Python/yt-dlp-Binärdateien
             auf manchen Geräten nicht zuverlässig. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- FileProvider für die APK-Selbstinstallation aus dem Download -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
